apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "my-notes.name" . }}-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.backend.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}