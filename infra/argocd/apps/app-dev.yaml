apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-notes-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dovgdanil/MyNotes.git
    targetRevision: main
    path: ./infra/helm/my-notes
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: my-notes-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
